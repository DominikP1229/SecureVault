<UserControl x:Class="SecureVault.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SecureVault.Views"
             Background="#1E1E1E">
    <UserControl.Resources>

        <!-- TŁO -->
        <SolidColorBrush x:Key="MainBackground" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="CardBackground" Color="#FF2C2F33"/>

        <!-- BUTTON -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="#3A3A3D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#505053"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- TEXT -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="RowBackground" Value="#1E293B"/>
            <Setter Property="AlternatingRowBackground" Value="#334155"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
        </Style>
    </UserControl.Resources>
    <Grid Background="{StaticResource MainBackground}">

        <Border Background="{StaticResource CardBackground}"
            CornerRadius="10"
            Padding="20"
            Margin="20">
            <Grid Margin="10">

        <StackPanel>
            <Button Content="Close"
        HorizontalAlignment="Right"
        Click="Close_Click"/>
            <TextBlock Text="Password History"
               Foreground="White"
               FontSize="24"
               FontWeight="Bold"
               Margin="0,0,0,15"/>

            <ListView Margin="5">

                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Date" Width="150"/>
                        <GridViewColumn Header="Password" Width="200"/>
                    </GridView>
                </ListView.View>

                <!-- MOCK -->
                <ListViewItem Content="2024-01-01   ******"/>
                <ListViewItem Content="2024-02-01   ******"/>

            </ListView>

        </StackPanel>

    </Grid>
        </Border>

    </Grid>
</UserControl>
