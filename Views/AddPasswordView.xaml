<UserControl x:Class="SecureVault.Views.AddPasswordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SecureVault.Views"
             mc:Ignorable="d" 
             >
    <Grid Margin="0,0,0,-286">
        <Border Grid.Column="1"
        Background="#2D2D30"
        CornerRadius="10"
        Padding="15"
        Margin="10,0,0,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Grid.Column="1" Margin="10">
                    <TextBlock Text="Title" Foreground="White" FontSize="18" Margin="0,0,0,10"/>
                    <TextBox Text="{Binding Title}" Margin="0,0,0,10"/>

                    <TextBlock Text="Username" Foreground="White" FontSize="18" Margin="0,0,0,10"/>
                    <TextBox Text="{Binding Username}" Margin="0,0,0,10"/>

                    <TextBlock Text="Category" Foreground="White" FontSize="18" Margin="0,0,0,10"/>
                    <TextBox Text="{Binding Category}" Margin="0,0,0,10"/>

                    <TextBlock Text="Password" Foreground="White" FontSize="18" Margin="0,0,0,10"/>
                    <TextBox Text="{Binding Password}" Margin="0,0,0,10"/>

                    <!-- ERRORS -->
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="White" Margin="0,0,0,10"/>

                    <!-- PASSWORD STRENGTH -->
                    <ProgressBar Height="8" Value="60" Margin="0,0,0,10"/>

                    <Button Content="Generate Password"/>

                    <!-- COPY BUTTONS -->
                    <StackPanel Orientation="Horizontal">
                        <Button Content="Copy Username"/>
                        <Button Content="Copy Password"/>
                        <Button Content="Copy URL"/>
                        <Button Content="Categories" Click="OpenCategories"/>
                    </StackPanel>

                    <TextBlock Text="Website" Foreground="White"/>
                    <TextBox Text="{Binding Website}" Margin="0,5,0,10"/>

                    <TextBlock Text="Notes" Foreground="White"/>
                    <TextBox Text="{Binding Notes}" Height="80" AcceptsReturn="True" Margin="0,5,0,10"/>

                    <!-- SAVE / CANCEL -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                        <Button Content="Save" Click="Save_Click" Width="80"/>
                        <Button Content="Cancel" Click="Cancel_Click" Width="80"/>
                    </StackPanel>

                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
