<UserControl x:Class="SecureVault.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:model="clr-namespace:SecureVault.Model"
             xmlns:viewmodel="clr-namespace:SecureVault.ViewModel"
             Background="#1E1E1E" Height="437" Width="955">

    <!-- ================= LAYOUT ================= -->
    <Grid Margin="10">
        <!--<Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" MinWidth="400"/>
            <ColumnDefinition Width="3*" MinWidth="450"/>
        </Grid.ColumnDefinitions> -->

        <!-- ================= LEFT SIDE ================= -->
        <DockPanel Grid.Column="0" LastChildFill="True">

            <!-- SEARCH + FILTER -->
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,5">
                <TextBox Width="200" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
                <ComboBox Width="150"
                          ItemsSource="{Binding FilterCategories}"
                          SelectedItem="{Binding SelectedFilterCategory}"
                          DisplayMemberPath="CategoryType"/>
            </StackPanel>

            <!-- DATA GRID -->
            <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding FilteredCredentials}" SelectedItem="{Binding SelectedCredential}" Margin="0,0,0,5" VerticalAlignment="Stretch" SelectionChanged="DataGrid_SelectionChanged" MouseDoubleClick="PasswordGrid_MouseDoubleClick">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*"/>
                    <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="*"/>
                    <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="*"/>
                    <DataGridTemplateColumn Header="Actions" Width="200">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">

                                    <Button Content="History" Margin="2" Padding="5,2" Click="OpenHistory"/>
                                    <Button Content="Edit" Click="Edit_Click" Margin="2" Padding="5,2"/>
                                    <Button Content="Delete" Click="Delete_Click" Margin="2" Padding="5,2"/>

                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <!-- ACTION BUTTONS -->
            <!--<StackPanel DockPanel.Dock="Bottom"
            Orientation="Horizontal"
            HorizontalAlignment="Center"
            Margin="0,10,0,0">

                <Button Content="Add" Command="{Binding AddCommand}" Width="80"/>
                <Button Content="Edit" Command="{Binding EditCommand}" Width="80"/>
                <Button Content="Delete" Command="{Binding DeleteCommand}" Width="80"/>

                <Button Content="Categories" Width="100" Click="OpenCategories"/>
                <Button Content="History" Width="100" Click="OpenHistory"/>
            </StackPanel> -->

        </DockPanel>

        <StackPanel
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,348,0,10" Width="570">
            <Button Content="Add" Width="100" Click="Add_Click"/>
            <Button Content="Delete" Width="100" Click="Delete_Click"/>
            <Button Content="Copy" Width="100" Click="Copy_Click"/>
            <Button Content="Categories" Width="100" Click="OpenCategories"/>
            <Button Content="Logout" Width="100" Click="Logout_Click"/>
        </StackPanel>

        <!-- ===== VIEW SWITCH AREA ===== -->
        <Border x:Name="SubViewContainer"
        Background="#AA000000"
        Visibility="Collapsed"
        Grid.ColumnSpan="2">

            <ContentControl x:Name="SubViewContent"/>
        </Border>
    </Grid>
</UserControl>
